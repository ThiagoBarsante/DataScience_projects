
SOURCE_FILE_DATA_DIR = table = crm.customer_info.customer_churn_bronze

Identify which features most influence SOURCE_FILE_DATA_DIR, guiding process improvements and ingredient selection.

Create a silver table and also build new features to target customer churn prediction

Read notebooks results at /Workspace/Users/<USER_EMAIL>/Customer_churn to get additinal information and EDA already executed

Build a baseline machine learning model using Random Forest to get feature importance and Gini Index... plot the results

create a notebook 03_silver_table_FE__and_ML_baseline